 <div class="card">
     <div class="card-body">
         <p class="fs-4"><span class="{{ $icon ?? "fa fa-user" }}"></span> {{ $title ?? "Title here" }}</p>
         <h2 class="fw-bold fs-2">{{ $data ?? 0 }}</h2>
     </div>
 </div>
